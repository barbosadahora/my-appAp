import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems:"center"
    },
    formLabel: {
        color: "#000000",
        fontSize: 18,
        paddingLeft: 20,
        fontWeight:'bold'
    },
    formValor:{
        color:"#1967d2",
        fontSize:20,
        fontWeight:'bold',
        paddingLeft: 20
    }
});

export default styles
